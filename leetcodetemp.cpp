#include <iostream>
#include <vector>

using namespace std;

void fastSort(vector<long> v){
    //get a pivot
    auto pivot = v[0];
    //partition
    for(auto element : v){
        if(element > )
    }

}